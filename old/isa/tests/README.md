# ISA Tooling Tests

The subdirectories here are reserved for generated and handwritten regression
fixtures:

- `decode_tests/`
- `encode_tests/`
- `canonical_tests/`
- `overlap_tests/`

The current smoke path is the validator plus generated metadata/artifact
commands documented in `../README.md`.
