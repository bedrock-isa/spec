selection: TLS symbol binding and placement provide a link-time constant byte offset from GS0.
base_register: isa:base.registers.SEGMENT.GS0
relocations:
- base.relocations.R_BEDROCK_TLS_OFFSET32S
- base.relocations.R_BEDROCK_TLS_OFFSET64
