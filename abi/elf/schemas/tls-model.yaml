$schema: https://json-schema.org/draft/2020-12/schema
$id: bedrock://schemas/elf-tls-model
title: Bedrock ELF TLS model
type: object
additionalProperties: true
required: [selection]
not: {required: [id]}
properties:
  selection: {type: string, minLength: 1}
