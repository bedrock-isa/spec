owner: base
description: >-
  Unprivileged; ID is a constant in 0 through 65535; zero and unavailable IDs fault with \texttt{INVALID\_SELECTOR}.
signature:
  result: u64
  parameters:
  - id: counter
    type: base.types.core.performance_counter
    constant: true
lowering:
  operation: isa:base.instructions.RDPMC
wrapper:
  kind: macro
