mnemonic: HALT
doc:
  title: Halt
  summary: Enter halted supervisor state until an enabled event resumes execution.
  description: Enter halted supervisor state until an enabled event resumes execution.
  instruction_family: core_control
  instruction_class: system
behavior:
  group: sentinels
  output: none
  operation:
  - arch_enter_halt();
attributes:
  privilege: supervisor
allocation:
  catalog_section: compact_primary
forms:
  operands: []
