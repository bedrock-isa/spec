mnemonic: FLSHDCACHE
doc:
  title: Flush Data Cache
  summary: Performs the flush data cache operation.
  description: Performs the flush data cache operation.
  instruction_family: cache
  instruction_class: system
behavior:
  group: cache
  inputs:
  - target
  output: none
  operation:
  - arch_cache_flush_data(target_v);
attributes:
  privilege:
    default: supervisor
    policy_controlled: true
allocation:
  catalog_section: system
  layout_group: CACHE_OPS
forms:
  operands:
  - name: target
    type: EA_or_range
