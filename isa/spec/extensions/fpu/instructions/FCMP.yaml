mnemonic: FCMP
doc:
  title: Floating-Point Compare
  summary: Performs the floating-point compare operation.
  description: Performs the floating-point compare operation.
  instruction_family: fpu
  instruction_class: fpu
behavior:
  group: fpu_move_compare
  inputs:
  - dst
  - src
  output: none
  operation:
  - ZF = dst_v f== src_v;
  - NF = dst_v f< src_v;
  - CF = 0;
  - VF = nan(dst_v) || nan(src_v);
  repflags: fpu_compare_flags
attributes:
  streaming_candidate: true
  fp_flags:
    update_when_required:
    - NV
allocation:
  catalog_section: fpu
  layout_group: FP_COMPARE
forms:
  operands:
  - - name: src
      type: FREG
    - name: dst
      type: FREG
  - - name: src
      type: EA
    - name: dst
      type: FREG
  size: S_D
  updates: FLAGS
  unordered: FLAGS.V = 1
