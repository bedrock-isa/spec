\clearpage
\section{Architectural Debug Triggers}\label{page:architectural-debug}

The Base debug facility defines stopping events, trigger configuration, and retry behavior for one
(:term:base.terms.logical_processor:). External debugger transport, authentication, forced halt and resume,
multicore coordination, and platform policy are outside the processor ISA.
