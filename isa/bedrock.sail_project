prelude {
  files
    foundations/architecture/prelude.sail,
    foundations/architecture/semantic_types.sail
}

catalog_types {
  requires prelude, operations
  files encoding/instruction/catalog_types.sail
}

decode {
  requires prelude, operations, catalog_types, catalog
  files
    encoding/instruction/framing.sail,
    encoding/instruction/selected.sail,
    encoding/instruction/types.sail,
    encoding/instruction/bytes.sail,
    addressing/effective_address/decode/effective_address.sail,
    encoding/instruction/decode.sail
}

fp {
  requires prelude, operations, catalog_types, catalog
  files
    instructions/semantics/floating_point/operation_catalog.sail,
    instructions/semantics/floating_point/transcendental_contract.sail,
    instructions/semantics/floating_point/register_pairs.sail,
    instructions/semantics/floating_point/types.sail,
    instructions/semantics/floating_point/environment.sail,
    instructions/semantics/floating_point/local_operations.sail,
    instructions/semantics/floating_point/request_contract.sail,
    instructions/semantics/floating_point/response_contract.sail,
    instructions/semantics/floating_point/finalize.sail
}

core {
  requires prelude, operations, catalog_types, catalog, decode, fp
  files
    execution/core/types.sail,
    execution/core/state.sail,
    execution/core/predicates.sail,
    addressing/effective_address/evaluation/effective_address.sail,
    execution/core/integer_bits.sail,
    instructions/semantics/integer/operands.sail,
    instructions/semantics/integer/arithmetic.sail,
    instructions/semantics/integer/data_control.sail,
    instructions/semantics/integer/routing.sail,
    system/state/system.sail,
    system/state/reset.sail,
    memory/access/transactions.sail,
    memory/cache/evaluations.sail,
    system/state/save_restore.sail,
    memory/cache/transactions.sail,
    memory/translation/model/translation.sail,
    system/requests/transactions.sail,
    addressing/effective_address/evaluation/transactions.sail,
    system/stack/transactions.sail,
    instructions/semantics/control/transactions.sail,
    system/events/events.sail,
    execution/repeat/repeat.sail,
    instructions/semantics/control/control.sail,
    instructions/semantics/floating_point/local_execution.sail,
    instructions/semantics/floating_point/transaction_inputs.sail,
    instructions/semantics/floating_point/transaction_compute.sail,
    instructions/semantics/vector/vector.sail,
    instructions/semantics/floating_point/transaction_flow.sail,
    execution/core/dispatch.sail,
    execution/core/resume_values.sail,
    execution/core/resume_restore.sail,
    execution/core/resume_common.sail,
    memory/access/continuation.sail,
    execution/core/resume_control.sail,
    execution/core/resume_system.sail,
    execution/core/resume_repeat.sail,
    execution/core/resume_events.sail,
    execution/core/resume_fp.sail,
    execution/core/resume.sail
}

postlude {
  requires prelude, operations, catalog_types, catalog, decode, fp, core, operation_entries
  files execution/core/boundary.sail
}

unit_tests {
  requires prelude, operations, catalog_types, catalog, decode, fp, core, operation_entries, postlude
  files
    tests/support.sail,
    tests/coverage.sail,
    tests/pairs.sail,
    tests/transactions.sail,
    tests/integer.sail,
    tests/vector.sail,
    tests/state.sail,
    tests/protocol_environment.sail,
    tests/protocol_state.sail,
    tests/protocol_stack.sail,
    tests/protocol_events.sail,
    tests/protocol.sail,
    tests/event_support.sail,
    tests/event.sail,
    tests/floating_point_support.sail,
    tests/floating_point_metadata.sail,
    tests/floating_point_values.sail,
    tests/floating_point_protocol.sail,
    tests/floating_point_conversion.sail,
    tests/floating_point.sail,
    tests/full.sail,
    tests/smoke.sail
}

main {
  requires prelude, operations, catalog_types, catalog, decode, fp, core, postlude, unit_tests
  files tests/main.sail
}
