\clearpage
\section{Instruction Encoding}\label{page:instruction-encoding}\label{section:instruction-header-formats}

\subsection{Instruction Stream and Record Boundary}
The byte addressed by PC is byte 0 of the current instruction. Instruction decoding determines the complete
encoded instruction length before operand evaluation begins.

The opcode space begins at byte 0, includes the header, and ends immediately before the first payload. Any appended
EA descriptor, displacement, immediate, bitmap, or instruction-specific value is a payload. Independently selected
appended units are separate payloads. Bytes after the required instruction length constitute padding.

The header is byte 0 for extrashort and short instructions and the first two bytes for medium and longer
instructions. It determines the encoded instruction length and selects the opcode space before operand evaluation
begins. An encoded instruction length that cannot contain the selected encoding raises
\texttt{ILLEGAL\_INSTRUCTION} before any architectural state is changed.

The required instruction length is the opcode-space length plus every required payload length. The encoded
instruction length must be at least that large. For an extended instruction, every trailing byte after the required
instruction length is uninterpreted padding; every byte value is valid. Padding never extends an operand, payload,
descriptor, or opcode space.

Before decode validation or operand evaluation, instruction fetch and permission checks cover the complete encoded
record, including padding. An inaccessible padding byte therefore reports the applicable instruction-fetch fault.
An undersized record raises \texttt{ILLEGAL\_INSTRUCTION.INSUFFICIENT\_LENGTH} before architectural state changes.

\subsection{Instruction Header Formats}

Instruction framing is byte-oriented. Byte 0 alone identifies extrashort and short instructions; extended
instructions use byte 0 and byte 1 to determine both encoded instruction length and opcode-space selection.

Extended byte0[5:2] is L, and the encoded instruction length is 3+L bytes. The opcode-space allocation bits begin at
byte0[1:0], continue through byte1, and then through later opcode-space bytes as required by the encoding class.

An extended instruction is invalid if its encoded instruction length is shorter than the opcode-space length: 3 bytes
for medium, 4 bytes for long, 5 bytes for extralong, and 6 bytes for xxlong.

The first eight allocation bits select the extended opcode space.
The value \texttt{11111111} selects the xxlong class, whose six-byte opcode space provides 42
allocation bits. The value \texttt{11111110} is reserved for custom extensions; it retains extralong framing and is
unallocated by the standard ISA. All other existing selectors retain their class and allocation.

\texttt{LEN n, <instruction>} requests an encoded extended instruction length of \texttt{n} bytes, where
\texttt{3 <= n <= 18}. The requested length must cover the required instruction length. Without \texttt{LEN}, the
assembler emits that required length. It fills requested trailing padding with zero. A disassembler emits
\texttt{LEN n,} when the encoded instruction length exceeds the required instruction length so reassembly preserves
the length; padding byte values are not preserved. Extrashort and short encodings retain their fixed lengths.

\begin{manuallistedbitdiagram}{Instruction Start Byte Formats}[fig:instruction-header-byte-formats]
\manualbitrow{extrashort byte}{%
\manualbitfixed{0}{1}
\manualbitfieldtext{ALLOC}{7}
}
\manualbitfieldrow{short bytes}{\manualbyterowlabels{0}{2}}{%
\manualbitfixed{10}{2}
\manualbitfieldtext{A-HI}{6}
\manualbitgap{1}
\manualbitfieldtext{A-LO}{8}
}
\manualbitfieldrow{extended start bytes}{\manualbyterowlabels{0}{2}}{%
\manualbitfixed{11}{2}
\manualbitfieldtext{L}{4}
\manualbitfieldtext{A}{2}
\manualbitgap{1}
\manualbitfieldtext{A-LO}{8}
}
\end{manuallistedbitdiagram}

Figure~\ref{fig:instruction-header-byte-formats} annotations are read from high to low bit. In byte 0, bit 7 distinguishes extrashort framing; bits 7..6
select short (\texttt{10}) or extended (\texttt{11}) framing. For short instructions, bits 5..2 and 1..0 continue
the opcode-space allocation bits. For extended instructions, bits 5..2 are \texttt{L}, bits 1..0 begin the
opcode-space allocation bits, and byte 1 continues them. Thus \texttt{L} encodes the encoded instruction length as
$3+L$ bytes directly in
Figure~\ref{fig:instruction-header-byte-formats}.

@INSTRUCTION_ENCODING_DIAGRAMS@

\manualtablecaption{Encoded Instruction Length Truth Table}
\begin{manuallongtable}{@{}>{\raggedright\arraybackslash}p{1.25in}>{\raggedright\arraybackslash}p{1.25in}>{\raggedright\arraybackslash}p{0.55in}@{}}
\toprule
\rowcolor{ManualHeaderFill}
\textbf{Byte 0 Pattern} & \textbf{Byte 1 Pattern} & \textbf{Bytes}\\
\midrule
\endfirsthead
\multicolumn{3}{l}{\scriptsize\itshape Table \themanualtable\ (continued)}\\
\toprule
\rowcolor{ManualHeaderFill}
\textbf{Byte 0 Pattern} & \textbf{Byte 1 Pattern} & \textbf{Bytes}\\
\midrule
\endhead
@INSTRUCTION_LENGTH_TRUTH_TABLE_ROWS@
\bottomrule
\end{manuallongtable}

\manualtablecaption{Encoding Class Summary}
\begin{manualtabular}{@{}>{\raggedright\arraybackslash}p{0.72in}>{\raggedright\arraybackslash}p{0.55in}>{\raggedright\arraybackslash}p{2.05in}>{\raggedright\arraybackslash}p{0.62in}>{\raggedright\arraybackslash}p{1.45in}@{}}
\toprule
\rowcolor{ManualHeaderFill}
\textbf{Class} & \textbf{Allocation Bits} & \textbf{Framing or Opcode Selector} & \textbf{Opcode-space Bytes} & \textbf{Validity}\\
\midrule
@ENCODING_CLASS_SUMMARY_ROWS@
\bottomrule
\end{manualtabular}

@INSTRUCTION_PAYLOAD_ORDERING_SECTION@

\input{isa/encoding/instruction/representative_instruction_encodings.tex}
