status: pending_formal_proof
target:
  afence_is_full_cumulative: true
  one_global_sc_order: true
  isolated_fenced_scalar_is_sc: true
  failed_seqcst_cmpxchg_is_sc_load: true
  compare_exchange_uses_order_join: true
  failed_release_component_creates_release_sequence: false
litmus_families:
- id: MP
  purpose: message passing through release/acquire and cumulative fences
- id: SB
  purpose: store buffering and sequentially consistent ordering
- id: LB
  purpose: load buffering under the weak baseline
- id: IRIW
  purpose: independent reads of independent writes and the global SC order
- id: RWC
  purpose: read-to-write causality and cumulative propagation
- id: release-sequence
  purpose: release-sequence formation and acquisition
- id: sc-fence
  purpose: sequentially consistent fences and fenced scalar accesses
- id: failed-compare-exchange
  purpose: failed compare-exchange ordering and absence of a release write
failure_action:
  - add_ordered_load_store
  - add_explicit_sc_fence
