\manualtablecaption{Core Builtins}
\begingroup\footnotesize
\setlength{\tabcolsep}{2pt}
\begin{longtable}{@{}p{1.25in}p{1.25in}p{0.8in}p{2.3in}@{}}
\toprule
\textbf{Name} & \textbf{C interface} & \textbf{Lowering} & \textbf{Availability, constraint, and effect}\\
\midrule
\endhead
\texttt{cpuid} & \texttt{u64(u64 selector)} & \texttt{CPUID} & User allowed; reads discovery state and not C memory.\\
\texttt{read\_status} & \texttt{uint16\_t(void)} & \texttt{RDSTATUS} & Unprivileged; reads \texttt{STATUS} and not C memory.\\
\texttt{rdpmc} & \texttt{uint64\_t(counter\_id)} & \texttt{RDPMC} & Unprivileged; ID is a constant in 0 through 65535; zero and unavailable IDs fault with \texttt{INVALID\_SELECTOR}.\\
\texttt{breakpoint} & \texttt{void(void)} & \texttt{BKPT} & Any privilege; always raises the architectural breakpoint/debug exception.\\
\texttt{trace} & \texttt{void(marker)} & \texttt{TRACE} & Unprivileged; marker is a constant in 0 through 65535; emits a marker when enabled and otherwise acts as a NOP.\\
\texttt{yield} & \texttt{void(void)} & \texttt{YIELD} & Any privilege; scheduling hint, not a compiler or memory barrier.\\
\texttt{wait} & \texttt{void(void)} & \texttt{WAIT} & Unprivileged PAUSE-like convergent hint; retires normally and permits a finite implementation-selected delay, including zero, before subsequent execution.\\
\bottomrule
\end{longtable}
\endgroup
