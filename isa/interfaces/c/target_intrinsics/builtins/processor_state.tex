\manualtablecaption{Processor-State Builtins}
\begingroup\footnotesize
\setlength{\tabcolsep}{2pt}
\begin{longtable}{@{}p{1.55in}p{1.35in}p{0.8in}p{1.9in}@{}}
\toprule
\textbf{Name} & \textbf{C interface} & \textbf{Lowering} & \textbf{Constraint and effect}\\
\midrule
\endhead
\texttt{save\_processor\_state} & \texttt{void(void *area)} & \texttt{SAVE} & Writable, CPUID-sized, 4096-byte-aligned area; writes architectural state and fully clobbers memory.\\
\texttt{restore\_processor\_state} & \texttt{void(const void *area)} & \texttt{RESTORE} & Readable, feature-valid, 4096-byte-aligned area; reads the complete selected processor-state image; may define \texttt{R0{-}{-}R15}, \texttt{FLAGS}, valid \texttt{GS} registers, and extension state; fully clobbers memory and prevents TLS or segmented memory accesses from moving across a restored \texttt{GS} addressing context.\\
\bottomrule
\end{longtable}
\endgroup
