\manualtablecaption{MMU Builtins}
\begingroup\footnotesize
\setlength{\tabcolsep}{2pt}
\begin{longtable}{@{}p{1.4in}p{1.35in}p{0.75in}p{2.1in}@{}}
\toprule
\textbf{Name} & \textbf{C interface} & \textbf{Lowering} & \textbf{Constraint and effect}\\
\midrule
\endhead
\texttt{invalidate\_tlb} & \texttt{void(void)} & \texttt{INVTLB} & Supervisor; translation-state side effect and full memory clobber.\\
\texttt{invalidate\_page} & \texttt{void(const void *)} & \texttt{INVPAGE} & Supervisor; argument supplies a linear address and invalidates any matching cached leaf mapping containing it; the operation fully clobbers memory.\\
\texttt{invalidate\_asid} & \texttt{void(asid)} & \texttt{INVASID} & Supervisor; ASID is a constant in 0 through 65535 and the operation fully clobbers memory.\\
\texttt{switch\_page\_table} & \texttt{void(uint64\_t ptcr)} & \texttt{SWPT} & Supervisor; valid page-table image required; changes address space and fully clobbers memory.\\
\texttt{switch\_page\_table\_asid} & \texttt{void(uint64\_t,uint16\_t)} & \texttt{SWPTA} & Supervisor; valid image and low 16-bit ASID; changes address space and fully clobbers memory.\\
\texttt{virtual\_to\_physical} & \texttt{result(address)} & \texttt{VTOP} & Supervisor; wrapper returns value and operation-defined \texttt{FLAGS} captured atomically; page walk without access to the translated C object.\\
\texttt{page\_table\_query} & \texttt{result(level,address)} & \texttt{PTQUERY} & Supervisor; level is an integer constant expression in 1 through 5; wrapper atomically returns value and \texttt{FLAGS}; page walk without access to the queried C object.\\
\bottomrule
\end{longtable}
\endgroup
