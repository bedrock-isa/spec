\documentclass[10pt,twoside]{article}
\newcommand{\BedrockArchitectureName}{Bedrock}
\newcommand{\BedrockDocumentTitle}{Bedrock Target Intrinsics}
\newcommand{\BedrockRunningTitle}{TARGET INTRINSICS}
\newcommand{\BedrockFooterTitle}{BEDROCK ARCHITECTURE}
\newcommand{\compilerbuiltin}[1]{\texttt{\detokenize{#1}}}
\newcommand{\compilerctype}[1]{\texttt{\detokenize{#1}}}
\input{isa/tex/bedrock-reference-common.tex}
\begin{document}
\BedrockMakeTitlePage{Bedrock Target Intrinsics}{C Compiler Builtins and Header Interfaces}{Version 1.0}
\BedrockFrontMatter

\clearpage
\section{Scope and Status}
This document defines the compiler-owned Bedrock C target interface: builtin
names, implementation-reserved header wrappers, source signatures, lowering
requirements, availability and privilege constraints, and compiler-visible
effects. It is the contract shared by a Bedrock-aware C frontend, optimizer,
backend, and the target headers.

Architectural instruction behavior remains defined by the Bedrock ISA
Specification. The baseline calling convention is defined by the Bedrock C
ABI. Far-pointer source types are defined by the Bedrock C Far-Pointer
Extensions, while their representation and calls are defined by the C ABI.
This document specifies how source programs reach those facilities; it does
not duplicate their architectural or language semantics.

\manualfield{Interface name:}{\texttt{bedrock-target-intrinsics}}
\manualfield{Source language:}{ISO C with implementation-reserved target interfaces}
\manualfield{Interface count:}{50 compiler builtins in 10 header families}

\section{Compiler Interface Model}

\subsection{Names and Ownership}
The compiler builtin namespace begins with
\texttt{\_\_builtin\_bedrock\_}. Each \textbf{Name} entry in this document
denotes that prefix followed by the displayed name. For example,
\texttt{cpuid} denotes
\texttt{\_\_builtin\_bedrock\_cpuid}. The ordinary header wrapper uses the
\texttt{\_\_bedrock\_} prefix with the same name unless its entry or the
far-pointer construction interface specifies a macro spelling.

All of these identifiers are implementation-reserved. A builtin is a compiler
primitive, not an external function symbol. The target headers own wrapper
declarations and umbrella include topology; this document owns their
ABI-visible meaning.

\subsection{Header Topology and Exposure}
\texttt{<bedrockintrin.h>} is the public umbrella. It includes the far, core,
memory, integer, and floating-point families and deliberately exposes no
system-management operation. \texttt{<bedrocksystemintrin.h>} is the system
umbrella for register, cache, MMU, processor-state, and virtualization
facilities. Any family header may also be included directly.

\manualtablecaption{Target Intrinsic Header Families}
\begingroup\footnotesize
\setlength{\tabcolsep}{2pt}
\begin{longtable}{@{}p{1.15in}p{1.65in}p{0.65in}p{2.15in}@{}}
\toprule
\textbf{Family} & \textbf{Header} & \textbf{Umbrella} & \textbf{Exposure}\\
\midrule
\endhead
far pointer & \texttt{<bedrockfarintrin.h>} & public & \texttt{bedrock-c-far} profile\\
core & \texttt{<bedrockcoreintrin.h>} & public & user and policy-controlled core operations\\
memory & \texttt{<bedrockmemoryintrin.h>} & public & user ordering and access hints\\
integer & \texttt{<bedrockintegerintrin.h>} & public & nonportable integer operations\\
floating point & \texttt{<bedrockfpuintrin.h>} & public & floating-point environment and classification\\
system registers & \texttt{<bedrocksysregintrin.h>} & system & supervisor or policy-controlled register access\\
cache & \texttt{<bedrockcacheintrin.h>} & system & cache management\\
MMU & \texttt{<bedrockmmuintrin.h>} & system & supervisor translation operations\\
processor state & \texttt{<bedrockstateintrin.h>} & system & runtime state management\\
virtualization & \texttt{<bedrockvirtintrin.h>} & system & optional virtualization acceleration\\
\bottomrule
\end{longtable}
\endgroup

\subsection{Common Compilation Rules}
Family wrappers are static inline functions unless an encoded immediate or
type operand requires a macro. Every builtin listed by an enabled profile must
be recognized by the compiler. If a required optional feature is disabled and
no fallback is specified here, compilation fails rather than emitting an
unresolved call.

An encoded immediate must be an integer constant expression in the stated
range. Header inclusion neither suppresses nor emulates architectural
privilege checks. An operation is volatile only when its entry states an
externally observable architectural effect. Compiler memory effects and
hardware ordering are exactly those stated by the entry; one does not imply
the other.

The \textbf{C interface} column uses
\compilerctype{result(arguments)} shorthand. Exact declarations are in the
named header; \texttt{u8}, \texttt{u16}, \texttt{u32}, and \texttt{u64}
abbreviate the corresponding \texttt{uintN\_t} types, and \texttt{result}
denotes \texttt{\_\_bedrock\_query\_result\_t}. A clobber means a compiler
memory clobber, while a named fence also has the hardware behavior defined by
the ISA.

\section{Public Target Interface}

\subsection{Far Pointer Builtins}
\manualfield{Header:}{\texttt{<bedrockfarintrin.h>}}
The four construction operations use the macro spellings
\texttt{\_\_BEDROCK\_FAR\_PTR\_INIT},
\texttt{\_\_BEDROCK\_FAR\_FLAT\_PTR\_INIT},
\texttt{\_\_BEDROCK\_FAR\_NULL}, and
\texttt{\_\_BEDROCK\_FAR\_PTR\_FROM\_SEGMENT}. Their pointer-type operand is
handled by the compiler and is not a runtime C value.

\manualtablecaption{Far Pointer Builtins}
\begingroup\footnotesize
\setlength{\tabcolsep}{2pt}
\begin{longtable}{@{}p{1.25in}p{1.25in}p{1.05in}p{2.05in}@{}}
\toprule
\textbf{Name} & \textbf{C interface} & \textbf{Lowering} & \textbf{Constraint and effect}\\
\midrule
\endhead
\compilerbuiltin{far_ptr_init} & \compilerctype{far T(type,address,image)} & value construction & Far type required; constant operands preserve constant-expression status; no fault, segment write, or memory access.\\
\compilerbuiltin{far_flat_ptr_init} & \compilerctype{far T(type,address)} & value construction & Far type required; attaches the canonical disabled image without faulting.\\
\compilerbuiltin{far_null} & \compilerctype{far T(type)} & constant construction & Far type required; produces the canonical all-zero representation.\\
\compilerbuiltin{far_ptr_from_segment} & \compilerctype{far T(type,offset,image)} & canonicalization & Far type required; invalid input produces an invalid representation without faulting.\\
\compilerbuiltin{far_address} & \compilerctype{uint64_t(pointer)} & address extraction & Accepts a far data or function pointer; pure extraction without validation or bounds probing.\\
\compilerbuiltin{far_same_encoding} & \compilerctype{int(left,right)} & two-word comparison & Compatible far pointer types required; pure representation comparison.\\
\bottomrule
\end{longtable}
\endgroup

\subsection{Core Builtins}
\manualfield{Header:}{\texttt{<bedrockcoreintrin.h>}}
\manualtablecaption{Core Builtins}
\begingroup\footnotesize
\setlength{\tabcolsep}{2pt}
\begin{longtable}{@{}p{1.25in}p{1.25in}p{0.8in}p{2.3in}@{}}
\toprule
\textbf{Name} & \textbf{C interface} & \textbf{Lowering} & \textbf{Availability, constraint, and effect}\\
\midrule
\endhead
\compilerbuiltin{cpuid} & \compilerctype{u64(u64 selector)} & \texttt{CPUID} & User allowed; reads discovery state and not C memory.\\
\compilerbuiltin{read_status} & \compilerctype{uint16_t(void)} & \texttt{RDSTATUS} & Unprivileged; reads \texttt{STATUS} and not C memory.\\
\compilerbuiltin{rdpmc} & \compilerctype{uint64_t(counter_id)} & \texttt{RDPMC} & Policy controlled; ID is a constant in 0 through 65535; reads the selected counter.\\
\compilerbuiltin{breakpoint} & \compilerctype{void(void)} & \texttt{BKPT} & Any privilege; always raises the architectural breakpoint/debug exception.\\
\compilerbuiltin{trace} & \compilerctype{void(marker)} & \texttt{TRACE} & Unprivileged; marker is a constant in 0 through 65535; emits a marker when enabled and otherwise acts as a NOP.\\
\compilerbuiltin{yield} & \compilerctype{void(void)} & \texttt{YIELD} & Any privilege; scheduling hint, not a compiler or memory barrier.\\
\compilerbuiltin{wait} & \compilerctype{void(void)} & \texttt{WAIT} & Policy controlled; may suspend until an event or interrupt and is a convergent side effect.\\
\bottomrule
\end{longtable}
\endgroup

\subsection{Memory Builtins}
\manualfield{Header:}{\texttt{<bedrockmemoryintrin.h>}}
\manualtablecaption{Memory Builtins}
\begingroup\footnotesize
\setlength{\tabcolsep}{2pt}
\begin{longtable}{@{}p{1.35in}p{1.3in}p{0.75in}p{2.2in}@{}}
\toprule
\textbf{Name} & \textbf{C interface} & \textbf{Lowering} & \textbf{Constraint and effect}\\
\midrule
\endhead
\compilerbuiltin{read_fence} & \compilerctype{void(void)} & \texttt{RFENCE} & Compiler and hardware read-ordering barrier.\\
\compilerbuiltin{write_fence} & \compilerctype{void(void)} & \texttt{WFENCE} & Compiler and hardware write-ordering barrier.\\
\compilerbuiltin{address_fence} & \compilerctype{void(void)} & \texttt{AFENCE} & Full compiler memory clobber and hardware address fence.\\
\compilerbuiltin{nontemporal_store_u8} & \compilerctype{void(u8 *,u8)} & \texttt{MOVNT.B} & One writable byte; one hinted store, neither volatile nor a fence.\\
\compilerbuiltin{nontemporal_store_u16} & \compilerctype{void(u16 *,u16)} & \texttt{MOVNT.W} & One writable 16-bit object; one hinted store, neither volatile nor a fence.\\
\compilerbuiltin{nontemporal_store_u32} & \compilerctype{void(u32 *,u32)} & \texttt{MOVNT.L} & One writable 32-bit object; one hinted store, neither volatile nor a fence.\\
\compilerbuiltin{nontemporal_store_u64} & \compilerctype{void(u64 *,u64)} & \texttt{MOVNT.Q} & One writable 64-bit object; one hinted store, neither volatile nor a fence.\\
\bottomrule
\end{longtable}
\endgroup

\subsection{Integer Builtins}
\manualfield{Header:}{\texttt{<bedrockintegerintrin.h>}}
\manualtablecaption{Integer Builtins}
\begingroup\footnotesize
\setlength{\tabcolsep}{2pt}
\begin{longtable}{@{}p{1.3in}p{1.55in}p{0.75in}p{2.0in}@{}}
\toprule
\textbf{Name} & \textbf{C interface} & \textbf{Lowering} & \textbf{Result and effect}\\
\midrule
\endhead
\compilerbuiltin{clmul_u8} & \compilerctype{u8(u8,u8)} & \texttt{CLMUL.B} & Pure; low 8 bits of the carryless product.\\
\compilerbuiltin{clmul_u16} & \compilerctype{u16(u16,u16)} & \texttt{CLMUL.W} & Pure; low 16 bits of the carryless product.\\
\compilerbuiltin{clmul_u32} & \compilerctype{u32(u32,u32)} & \texttt{CLMUL.L} & Pure; low 32 bits of the carryless product.\\
\compilerbuiltin{clmul_u64} & \compilerctype{u64(u64,u64)} & \texttt{CLMUL.Q} & Pure; low 64 bits of the carryless product.\\
\bottomrule
\end{longtable}
\endgroup

\subsection{Floating-Point Builtins}
\manualfield{Header:}{\texttt{<bedrockfpuintrin.h>}}
\manualtablecaption{Floating-Point Builtins}
\begingroup\footnotesize
\setlength{\tabcolsep}{2pt}
\begin{longtable}{@{}p{1.25in}p{1.3in}p{0.85in}p{2.2in}@{}}
\toprule
\textbf{Name} & \textbf{C interface} & \textbf{Lowering} & \textbf{Availability, constraint, and effect}\\
\midrule
\endhead
\compilerbuiltin{read_fstatus} & \compilerctype{uint16_t(void)} & \texttt{RDFSTATUS} & Unprivileged; access to the floating-point environment.\\
\compilerbuiltin{write_fstatus} & \compilerctype{void(uint16_t)} & \texttt{WRFSTATUS} & Unprivileged; reserved bits follow the ISA; changes the floating-point environment.\\
\compilerbuiltin{read_fflags} & \compilerctype{uint16_t(void)} & \texttt{RDFFLAGS} & Unprivileged; reads accrued floating-point exception flags.\\
\compilerbuiltin{write_fflags} & \compilerctype{void(uint16_t)} & \texttt{WRFFLAGS} & Unprivileged; reserved bits follow the ISA; writes accrued flags.\\
\compilerbuiltin{fclass_f32} & \compilerctype{uint16_t(float)} & \texttt{FCLASS.S} & FPU required; pure ten-class one-hot classification; does not update \texttt{FFLAGS}.\\
\compilerbuiltin{fclass_f64} & \compilerctype{uint16_t(double)} & \texttt{FCLASS.D} & FPU required; pure ten-class one-hot classification; does not update \texttt{FFLAGS}.\\
\bottomrule
\end{longtable}
\endgroup

\section{System Target Interface}
The system umbrella is separate so ordinary source inclusion does not
accidentally make privileged facilities appear available. The compiler emits
the requested architectural operation; runtime privilege and policy checks
remain active.

\subsection{System-Register Builtins}
\manualfield{Header:}{\texttt{<bedrocksysregintrin.h>}}
\manualtablecaption{System-Register Builtins}
\begingroup\footnotesize
\setlength{\tabcolsep}{2pt}
\begin{longtable}{@{}p{1.4in}p{1.3in}p{0.7in}p{2.2in}@{}}
\toprule
\textbf{Name} & \textbf{C interface} & \textbf{Lowering} & \textbf{Availability, constraint, and effect}\\
\midrule
\endhead
\compilerbuiltin{write_status} & \compilerctype{void(uint16_t)} & \texttt{WRSTATUS} & Supervisor; reserved and privilege-controlled bits follow the ISA; compiler-visible side effect.\\
\compilerbuiltin{read_control_register} & \compilerctype{uint64_t(selector)} & \texttt{RDCR} & Supervisor; selector is a constant in 0 through 65535 naming a defined register.\\
\compilerbuiltin{write_control_register} & \compilerctype{void(selector,u64)} & \texttt{WRCR} & Supervisor; defined constant selector required; writes the register and clobbers memory.\\
\compilerbuiltin{read_segment_register} & \compilerctype{uint64_t(selector)} & \texttt{RDSEG} & Policy controlled; selector is a compile-time \texttt{\_\_bedrock\_segment\_register\_t}.\\
\compilerbuiltin{write_segment_register} & \compilerctype{void(selector,image)} & \texttt{WRSEG} & Policy controlled; general \texttt{CS} writes forbidden; validates the image, changes address interpretation, and fully clobbers memory.\\
\bottomrule
\end{longtable}
\endgroup

\subsection{Cache-Management Builtins}
\manualfield{Header:}{\texttt{<bedrockcacheintrin.h>}}
All range operations evaluate the address once. A zero length is a no-op; a
nonzero length selects exactly that byte range and may lower to one or more EA
or range forms.

\manualtablecaption{Cache-Management Builtins}
\begingroup\footnotesize
\setlength{\tabcolsep}{2pt}
\begin{longtable}{@{}p{1.45in}p{1.3in}p{0.9in}p{1.95in}@{}}
\toprule
\textbf{Name} & \textbf{C interface} & \textbf{Lowering} & \textbf{Availability and effect}\\
\midrule
\endhead
\compilerbuiltin{flush_dcache} & \compilerctype{void(void *,size_t)} & \texttt{FLSHDCACHE} & Supervisor by default, policy controlled; cache side effect and full memory clobber.\\
\compilerbuiltin{invalidate_dcache} & \compilerctype{void(void *,size_t)} & \texttt{INVDCACHE} & Supervisor by default, policy controlled; cache side effect and full memory clobber.\\
\compilerbuiltin{invalidate_icache} & \compilerctype{void(void *,size_t)} & \texttt{INVICACHE} & Supervisor by default, policy controlled; instruction-cache side effect and instruction-stream barrier.\\
\compilerbuiltin{writeback_dcache} & \compilerctype{void(void *,size_t)} & \texttt{WRBKDCACHE} & Supervisor by default, policy controlled; cache side effect and full memory clobber.\\
\compilerbuiltin{sync_cache} & \compilerctype{void(void *,size_t)} & \texttt{SYNCCACHE} & Supervisor by default, policy controlled; synchronizes data/instruction visibility and the instruction stream.\\
\bottomrule
\end{longtable}
\endgroup

\subsection{MMU Builtins}
\manualfield{Header:}{\texttt{<bedrockmmuintrin.h>}}
\manualtablecaption{MMU Builtins}
\begingroup\footnotesize
\setlength{\tabcolsep}{2pt}
\begin{longtable}{@{}p{1.4in}p{1.35in}p{0.75in}p{2.1in}@{}}
\toprule
\textbf{Name} & \textbf{C interface} & \textbf{Lowering} & \textbf{Constraint and effect}\\
\midrule
\endhead
\compilerbuiltin{invalidate_tlb} & \compilerctype{void(void)} & \texttt{INVTLB} & Supervisor; serializing invalidation and full memory clobber.\\
\compilerbuiltin{invalidate_page} & \compilerctype{void(const void *)} & \texttt{INVPAGE} & Supervisor; argument supplies the linear page address; serializing and fully clobbering.\\
\compilerbuiltin{invalidate_asid} & \compilerctype{void(asid)} & \texttt{INVASID} & Supervisor; ASID is a constant in 0 through 65535; serializing and fully clobbering.\\
\compilerbuiltin{switch_page_table} & \compilerctype{void(uint64_t ptcr)} & \texttt{SWPT} & Supervisor; valid page-table image required; changes address space and fully clobbers memory.\\
\compilerbuiltin{switch_page_table_asid} & \compilerctype{void(uint64_t,uint16_t)} & \texttt{SWPTA} & Supervisor; valid image and low 16-bit ASID; changes address space and fully clobbers memory.\\
\compilerbuiltin{virtual_to_physical} & \compilerctype{result(address)} & \texttt{VTOP} & Supervisor; wrapper returns value and operation-defined \texttt{FLAGS} captured atomically; page walk without access to the translated C object.\\
\compilerbuiltin{page_table_query} & \compilerctype{result(level,address)} & \texttt{PTQUERY} & Supervisor; level is a constant in 0 through 7; wrapper atomically returns value and \texttt{FLAGS}; page walk without access to the queried C object.\\
\bottomrule
\end{longtable}
\endgroup

\Needspace{2.0in}
\subsection{Processor-State Builtins}
\manualfield{Header:}{\texttt{<bedrockstateintrin.h>}}
\manualtablecaption{Processor-State Builtins}
\begingroup\footnotesize
\setlength{\tabcolsep}{2pt}
\begin{longtable}{@{}p{1.55in}p{1.35in}p{0.8in}p{1.9in}@{}}
\toprule
\textbf{Name} & \textbf{C interface} & \textbf{Lowering} & \textbf{Constraint and effect}\\
\midrule
\endhead
\compilerbuiltin{save_processor_state} & \compilerctype{void(void *area)} & \texttt{SAVE} & Writable, CPUID-sized, 4096-byte-aligned area; writes architectural state and fully clobbers memory.\\
\compilerbuiltin{restore_processor_state} & \compilerctype{void(const void *area)} & backend restore & Readable, feature-valid, 4096-byte-aligned area; may define \texttt{R0--R15}, \texttt{FLAGS}, valid \texttt{GS} registers, and extension state.\\
\bottomrule
\end{longtable}
\endgroup

Both operations are unprivileged, but restoration of \texttt{STATUS} remains
privilege-restricted. The backend must model every restored register
explicitly rather than treating restore as an opaque call or memory effect.

\subsection{Virtualization-Acceleration Builtin}
\manualfield{Header:}{\texttt{<bedrockvirtintrin.h>}}
\manualtablecaption{Virtualization-Acceleration Builtin}
\begingroup\footnotesize
\setlength{\tabcolsep}{2pt}
\begin{longtable}{@{}p{1.45in}p{1.45in}p{0.8in}p{1.9in}@{}}
\toprule
\textbf{Name} & \textbf{C interface} & \textbf{Lowering} & \textbf{Availability, constraint, and effect}\\
\midrule
\endhead
\compilerbuiltin{encode_instruction} & \compilerctype{intptr_t(dst,c,o1,o2,o3)} & \texttt{ENCINST} & \texttt{VIRTACCEL} required; initialized descriptor and sufficient destination space; writes canonical instruction bytes.\\
\bottomrule
\end{longtable}
\endgroup

The wrapper accepts a
\texttt{\_\_bedrock\_instruction\_descriptor\_t}, assigns its control and
three operand words to the builtin's implicit \texttt{R0--R3} inputs, and
captures the \texttt{R0} result. Executing newly written code still requires
an explicit \texttt{SYNCCACHE}.

\section{Shared Header Types and Exclusions}
\manualtablecaption{Target Intrinsic Shared Types}
\begingroup\footnotesize
\setlength{\tabcolsep}{2pt}
\begin{longtable}{@{}p{2.05in}p{3.45in}@{}}
\toprule
\textbf{Type} & \textbf{ABI contract}\\
\midrule
\endhead
\texttt{\_\_bedrock\_segment\_register\_t} & selectors for \texttt{CS}, \texttt{DS}, \texttt{SS}, and \texttt{GS0} through \texttt{GS4}\\
\texttt{\_\_bedrock\_query\_result\_t} & \compilerctype{u64 value; u16 flags; u16 reserved[3]}\\
\texttt{\_\_bedrock\_instruction\_descriptor\_t} & \compilerctype{u64 control, operand1, operand2, operand3}\\
\bottomrule
\end{longtable}
\endgroup

\smallskip
\begingroup\small
\noindent\textbf{Deliberate exclusions.}
No target builtin is defined for near/far conversion or far-address
truncation; \texttt{EXTRACT}, \texttt{CLMULH}, \texttt{MULHS},
\texttt{MULHSU}, or \texttt{MULHU}; raw integer \texttt{FLAGS} manipulation;
or call, return, stack, and repeat-control instructions.
\endgroup

\end{document}
