# Effective addresses

`definition.yaml` owns the effective-address encoding grammar. `decode/` parses
that grammar from instruction records, `evaluation/` evaluates decoded
addresses and address-producing instructions, and `manual/` contains the
downstream explanatory TeX sources and generated-fragment inputs.
