\begin{manualformblock}{0.70in}
\begin{tabular}{@{}l@{}}
; memmove(dst, src, n), non-overlapping or forward byte copy\\
; R0 = src, R1 = dst\\
; R2 = n\\
REP R2, (MOV.B [R0++], [R1++])\\
\end{tabular}
\end{manualformblock}
