\clearpage
\subsection{Page-Table Entry Format}\label{section:page-table-entry-format}

Every page-table entry is 64 bits. The low twelve bits contain hardware-defined traversal, permission, and memory
attributes. The physical frame number occupies bits \texttt{PABITS-1..12}; bits above the selected physical-address
width are available to software and ignored by the page-table walker.

\newcommand{\manualpteattributefields}{%
\manualformatfield{T}{1}
\manualformatfield{S}{1}
\manualformatfield{C}{2}
\manualformatfield{M}{1}
\manualformatfield{D}{1}
\manualformatfield{A}{1}
\manualformatfield{G}{1}
\manualformatfield{U}{1}
\manualformatfield{X}{1}
\manualformatfield{W}{1}
\manualformatfield{P}{1}
}

\begin{manuallistedformatdiagram}{Page-Table Entry Formats}
\manualformatrowrange{PABITS=48}{63}{0}{%
\manualformatfield{software}{16}
\manualformatfield{PFN}{36}
\manualpteattributefields
}
\manualformatrowrange{PABITS=56}{63}{0}{%
\manualformatfield{software}{8}
\manualformatfield{PFN}{44}
\manualpteattributefields
}
\end{manuallistedformatdiagram}
{\small
In both rows, \texttt{S}, \texttt{C}, and \texttt{M} abbreviate
\texttt{SW0}, \texttt{CP}, and \texttt{AT}; the other labels match the field table.
\par}

\manualtablecaption{Page-Table Entry Fields}
\begin{manualtabular}{@{}>{\raggedright\arraybackslash}p{0.65in}>{\raggedright\arraybackslash}p{0.85in}>{\raggedright\arraybackslash}p{3.85in}@{}}
\toprule
\rowcolor{ManualHeaderFill}
\textbf{Field} & \textbf{Bits} & \textbf{Meaning}\\
\midrule
\texttt{P} & \texttt{0} & present\\
\texttt{W} & \texttt{1} & writable\\
\texttt{X} & \texttt{2} & executable\\
\texttt{U} & \texttt{3} & user-domain mapping\\
\texttt{G} & \texttt{4} & global translation\\
\texttt{A} & \texttt{5} & accessed; may be set on a consumed byte-addressed entry, including a speculative traversal; required one for a slot leaf\\
\texttt{D} & \texttt{6} & byte-addressed leaf dirty bit, set exactly when a store or atomic update commits; required one for a slot leaf\\
\texttt{AT} & \texttt{7} & 0 byte-addressed leaf; 1 externally acknowledged slot-addressed leaf\\
\texttt{CP} & \texttt{9..8} & 0 cacheable; 1 uncacheable; 2 write-through; 3 write-combining\\
\texttt{SW0} & \texttt{10} & software-defined and ignored by hardware\\
\texttt{T} & \texttt{11} & one for a non-leaf table pointer; zero for an L1 leaf\\
\texttt{PFN} & \texttt{PABITS-1..12} & next-table or mapped-page physical frame number\\
software & \texttt{63..PABITS} & software-defined and ignored by hardware\\
\bottomrule
\end{manualtabular}

At every level above L1, P and T must both be one; D, G, and AT must be zero. At L1, P must be one and T must be
zero. W, X, and U permissions are accumulated across the walk. A reserved or malformed consumed entry raises
\texttt{PAGE\_FAULT}. Supervisor current-domain access to a user mapping faults; the checked user-domain operations
select the user-domain permission path explicitly.

An \texttt{AT=1} leaf must have \texttt{CP=1} (uncacheable), \texttt{X=0}, \texttt{A=1}, and \texttt{D=1}.
Any \texttt{AT=1} leaf with inconsistent attributes is a malformed PTE. Because A and D are required to be set in a
valid slot leaf, an access never performs a speculative A update or a commit-time D update to that leaf.

When hardware changes A or D from zero to one, it performs a conditional 64-bit atomic read-modify-write of the
complete PTE. The update sets only the required A or D bits and preserves every other bit from the current 64-bit
value. It succeeds only if the translation-relevant PTE value still matches the value that was validated. If a
concurrent write changes that value first, the walker must re-read and revalidate the entry; it must not write back
stale PTE fields.

A may be set after a structurally valid entry is consumed by a speculative walk even when the initiating
instruction is later squashed or faults after that traversal. D is exact rather than speculative: a failed,
squashed, permission-denied, or non-storing operation must not change D. A store or successful atomic memory update
may become architecturally visible only if any required D update also succeeds. PTQUERY and VTOP retain their
instruction-specific rule that they never modify A or D.
