\begin{manualformblock}{0.70in}
\begin{tabular}{@{}l@{}}
; memcpy(dst, src, n), byte copy forward\\
; R0 = src, R1 = dst\\
; R2 = n\\
REP R2, (MOV.B [R0++], [R1++])\\
\end{tabular}
\end{manualformblock}
