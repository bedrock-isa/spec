\clearpage
\section{Effective Addressing Modes}

An effective-address field is an operand descriptor. It may select a register value, an immediate value, or a memory
operand pointer. Only memory forms continue into segment pre-translation and paging.

The seven-bit compact EA field is embedded in the opcode payload. Any displacement, absolute address, immediate, or
EXT0 descriptor bytes are appended as EA payload bytes. When an instruction contains multiple payload-bearing
operands, payloads are consumed in instruction operand order.

Multi-byte scalar EA payloads, such as displacements, absolute addresses, and immediates, are little-endian. Payload
names ending in s are signed and are sign-extended before use; payload names without s are unsigned unless the
consuming instruction explicitly says otherwise. An EXT0 descriptor is not a multi-byte scalar: its descriptor bytes
are consumed in the displayed stream order, byte 0 followed by byte 1, and bit numbering restarts at 7 for each byte.

Indexed scale is implicit in the consuming instruction. For ordinary B/W/L/Q memory operations, the scale is 1, 2,
4, or 8. LEA uses its suffix to select the scale.

\clearpage
\subsection{Compact EA Addressing Modes}
Compact EA forms are the fast-path operand encodings. They cover direct Rn and SP operands, common Rn/SP/PC memory
operands, absolute memory operands, immediate operands, and the EXT0 escape.

Compact memory forms that do not name SP or PC use the operation default data segment. SP memory forms are fixed to
SS, and PC memory forms are fixed to CS.

\input{isa/tools/latex_builder/templates/fragments/compact_ea_reference_blocks.tex}

@COMPACT_EA_TABLE@

\clearpage
\subsection{Extended EA Descriptor}
A compact EXT0 EA value selects an extended descriptor and the displacement width. The descriptor bytes then select
segment, base, index, zero-base, SP/PC indexed, and auto-update behavior.

EXT0 has no default segment as a descriptor class. A concrete descriptor either encodes SEG(s), selects fixed SS/CS
through SP or PC, or explicitly uses the operation default data segment.

\input{isa/tools/latex_builder/templates/fragments/ea_field_diagrams.tex}

\begin{manuallongtable}{@{}>{\raggedright\arraybackslash}p{0.95in}>{\raggedright\arraybackslash}p{0.45in}>{\raggedright\arraybackslash}p{0.70in}>{\raggedright\arraybackslash}p{3.30in}@{}}
\toprule
\textbf{Payload} & \textbf{Bytes} & \textbf{Value} & \textbf{Use}\\
\midrule
\endhead
disp8s & 1 & signed & displacement added to base/index expression\\
disp16s & 2 & signed & displacement added to base/index expression\\
disp32s & 4 & signed & displacement added to base/index expression\\
disp64 & 8 & unsigned & displacement added to base/index expression\\
abs32s & 4 & signed & absolute address, sign-extended before use\\
abs64 & 8 & unsigned & absolute address payload\\
imm8s & 1 & signed & immediate operand payload\\
imm16s & 2 & signed & immediate operand payload\\
imm32s & 4 & signed & immediate operand payload\\
imm64 & 8 & unsigned & immediate operand payload\\
EXT0 descriptor & 1 or 2 & encoded & extended EA descriptor; present only for EXT0 escapes\\
\bottomrule
\end{manuallongtable}
\manualtablecaption{EA Payload Names}

For a compact displacement, absolute address, or immediate, the selected payload follows the EA field at that
operand's payload position. EXT0 begins with the compact escape value and one or two descriptor bytes; a displaced
EXT0 form places its selected displacement after those descriptor bytes. When an instruction has more than one
payload-bearing EA operand, the decoder consumes each complete EA payload sequence in instruction operand order.

@EXT0_SECTION@

@AUTO_UPDATE_SECTION@
