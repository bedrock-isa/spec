\clearpage
\section{Exception Processing Reference}

Exception processing covers synchronous restartable CPU exceptions, traps, NMIs, external interrupts, and the
supervisor-entry state needed to return from them.

The processor records a restartable context for CPU exceptions. The operating system decides whether that
context is retried, emulated, reported, or terminated.

\subsection{Interrupt Vector Table}
The interrupt vector table occupies one 4096-byte page selected by ICR.ivt\_page. It contains 256 fixed-size
entries, each 16 bytes. The vector number selects the entry at ICR.ivt\_page * 4096 + vector * 16.

Bytes 0 through 7 contain the 64-bit interrupt handler address. Byte 8 is the entry control byte. HP is the
handler-present bit; if it is clear, delivery immediately becomes DOUBLE\_FAULT. SN selects the supervisor stack
set used for the entry frame. Reserved IVT bits must be written as zero.

\input{isa/tools/latex_builder/templates/fragments/ivt_entry_diagrams.tex}

\input{isa/tools/latex_builder/templates/fragments/ivt_entry_table.tex}

\input{isa/tools/latex_builder/templates/fragments/supervisor_stack_sets.tex}

\subsection{Vector Assignment}
Vectors 0x00..0x1F are owned by the CPU architecture. Vectors 0x20..0x3F are reserved for future architectural
use. Operating systems, platforms, and devices may assign vectors 0x40..0xFF.

SYSCALL does not allocate or consume an IVT vector. It enters supervisor mode through the SPC/SCS/SDS
supervisor-entry path.

\input{isa/tools/latex_builder/templates/fragments/vector_range_table.tex}

\input{isa/tools/latex_builder/templates/fragments/cpu_vector_table.tex}

\subsection{Entry and Return Rules}
Exception entry is precise at the architectural instruction boundary or at the instruction-defined restart point.
Completed earlier instructions remain committed; the faulting operation reports enough state for restart or
emulation.

IRET restores the saved execution context after validating the saved segment and status fields. Invalid saved
state raises the appropriate control-state exception instead of partially returning.

The frame save and the transition to supervisor mode are atomic with respect to architectural visibility. Entry sets
STATUS.PM to one at its commit point, and return restores the saved STATUS image only after validation. SYSRET accepts
only a syscall frame; IRET handles interrupt, trap, and exception frames.

An IVT entry with HP clear immediately redirects delivery to DOUBLE\_FAULT. Attempting to deliver a reserved
CPU-owned vector has the same result. An invalid instruction length is reported as ILLEGAL\_INSTRUCTION rather than
as a separate length fault, while segment-bound and canonical-address failures use PAGE\_FAULT.

@EXCEPTION_INSTRUCTION_TABLE@

\clearpage
\subsection{Supervisor Entry Stack Frame}
The fixed supervisor-entry frame is 64 bytes: eight 64-bit slots. FRAME\_CONTROL.frame\_size records the total
frame size in 8-byte units, so the base frame records 8 units before any optional payload.

Offsets are measured from the saved stack pointer. Optional payload, when present, follows the fixed frame in
whole 64-byte blocks.

\input{isa/tools/latex_builder/templates/fragments/supervisor_stack_frame_diagram.tex}

\input{isa/tools/latex_builder/templates/fragments/stack_frame_table.tex}

\texttt{FRAME\_CONTROL.frame\_type} documents the stack frame format. Payload, if any, is appended after the
fixed frame in whole 64-byte blocks. \texttt{FRAME\_CONTROL.frame\_size} gives the total number of 8-byte slots,
and the frame type code determines how software interprets the allocated payload blocks. IRET does not validate
the frame type.

\input{isa/tools/latex_builder/templates/fragments/frame_type_table.tex}

\texttt{FRAME\_CONTROL} packs the saved FLAGS image and STATUS image with the frame metadata. The fixed frame
also reserves two 64-bit extension slots for future architected base-frame state.

\input{isa/tools/latex_builder/templates/fragments/payload_slot_table.tex}

\input{isa/tools/latex_builder/templates/fragments/frame_control_diagram.tex}

\input{isa/tools/latex_builder/templates/fragments/frame_control_table.tex}

\Needspace{6.4in}
\subsection{Repeat Fault Continuation}
\begin{samepage}
When FRAME\_CONTROL.rep\_fault is set, the FAULT\_AUX payload records the active repeat context saved by an
interrupt, exception, or fault. Handler entry clears architectural repeat state; IRET restores the saved repeat
state together with the saved PC and status state.

During single-instruction repeat, the saved PC is the repeated body instruction boundary. For REPG, software
reconstructs the group start from the saved PC and the encoded byte-distance group-start delta. Returning with
IRET preserves the continuation state unless software edits the frame before return.

\input{isa/tools/latex_builder/templates/fragments/repeat_fault_aux_diagram.tex}

\input{isa/tools/latex_builder/templates/fragments/repeat_fault_aux_table.tex}
\end{samepage}

\subsection{Interrupt Reset State}

\input{isa/tools/latex_builder/templates/fragments/reset_state_table.tex}

Cold reset initializes the processor into a supervisor-controlled state with interrupts disabled. The platform
supplies BOOTPC and BOOTCFG, clears the remaining resettable state, and begins execution at BOOTPC. The RESET
instruction performs a warm reset: it preserves BOOTPC and BOOTCFG, clears the remaining resettable state, and resumes
at BOOTPC. Software must initialize segment state, translation controls, interrupt-vector state, and any enabled
extension state before entering ordinary user execution.
