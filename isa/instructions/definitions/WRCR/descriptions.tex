WRCR validates and atomically writes the selected (:term:base.terms.control_register:) in supervisor mode. The WRCR Selector Rules table defines each writable image, validation condition, and translation or event-state side effect.
