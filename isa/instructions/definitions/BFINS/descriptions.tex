Replaces 	exttt{imm6(w)+1} destination bits beginning at bit 	exttt{imm6(l)} with the same number of low source
bits. Destination bits outside the field are preserved. An encoding is invalid when the selected field extends
past the operand width.
