mnemonic: BTEST
name: Bit Test
summary: Tests the indexed destination bit without writing it, sets Z when it is clear, and clears NCV.
route: integer_bitfield
privileged: false
repeat:
  type: repcc
  observed_value: computed
operands:
  bit_index:
    role: source
    access: read
    value_type: integer
  dst:
    role: source
    access: read
    value_type: integer
