BNDUXX treats both bounds as exclusive. It sets V when unsigned(value) is outside (unsigned(lo), unsigned(hi)) and clears Z, N, and C. The B-sized 0 or 1 violation result supplies the REPcc observed value.
