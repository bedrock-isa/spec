mnemonic: INCF
title: Increment And Set Flags
summary: Increments the destination operand and updates integer FLAGS.
description: Adds one to the selected-width destination, writes the modular result, and updates Z, N, C, and V. REPcc observes the written destination result rather than architectural FLAGS.
attributes:
  class: integer
  family: integer_unary
  privilege: unprivileged
repeat:
  contexts:
  - REP
  - REPcc
  - REPG
  observed:
    kind: result
    operand: dst
flag_effects:
  FLAGS:
    Z: "result == 0"
    N: "most_significant_bit(result)"
    C: "unsigned carry out"
    V: "signed overflow"
