mnemonic: MULHS
title: Signed Multiply High
summary: Performs the signed multiply high operation.
description: Multiplies the 64-bit operands as signed integers and writes the high 64 bits of the 128-bit product.
attributes:
  class: integer
  family: integer_mul_div
  privilege: unprivileged
repeat:
  contexts:
  - REP
  - REPcc
  observed:
    kind: result
    operand: dst
