WAIT is a PAUSE-like execution hint. It retires as an ordinary instruction with the following instruction as its
continuation. The implementation selects a finite delay before subsequent execution; the selected delay may be zero.
The architectural result of WAIT is normal retirement to the recorded continuation.
