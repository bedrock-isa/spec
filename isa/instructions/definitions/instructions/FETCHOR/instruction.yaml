mnemonic: FETCHOR
title: Atomic Fetch Or
summary: Performs the atomic fetch or operation.
description: Atomically replaces the memory operand with the selected-width bitwise disjunction of the old memory value and the source, and returns the old memory value in the source register.
attributes:
  class: system
  family: atomics
  privilege: unprivileged
