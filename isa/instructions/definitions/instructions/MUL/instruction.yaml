mnemonic: MUL
title: Multiply Low
summary: Multiplies the destination by the source and keeps the low half.
description: MUL multiplies the selected-width operands and writes the low N bits of the product.
attributes:
  class: integer
  family: integer_mul_div
  privilege: unprivileged
repeat:
  contexts:
  - REP
  - REPcc
  observed:
    kind: result
    operand: dst
