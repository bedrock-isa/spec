Computes and translates the address-role EA without fetching from it, selects its CPUID-sized maintenance block, invalidates the executing logical processor\textquotesingle{}s matching instruction-cache, decoded, and prefetch state, and serializes later instruction fetch.
