Tests the destination bit selected by the low six bits of the bit index and writes Z from whether that bit was clear while clearing N, C, and V, without changing the destination. REPcc observes the old tested bit as a B-sized 0 or 1 rather than architectural FLAGS.
