Tests the destination bit selected by the low six bits of the bit index and writes Z from whether that bit was clear while clearing N, C, and V, without changing the destination. The B-sized 0 or 1 value of the old tested bit supplies the REPcc observed value.
