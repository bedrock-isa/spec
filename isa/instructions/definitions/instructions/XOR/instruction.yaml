mnemonic: XOR
title: Bitwise Xor
summary: Computes the bitwise XOR of the source and destination operands.
description: Writes the selected-width bitwise exclusive-or of the source and destination values to the destination.
attributes:
  class: integer
  family: integer_alu
  privilege: unprivileged
repeat:
  contexts:
  - REP
  - REPcc
  observed:
    kind: result
    operand: dst
