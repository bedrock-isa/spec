ADD reads the source and destination operands at the selected integer width,
adds their values modulo that width, and writes the low result bits to the
destination. The common integer result-extension rule defines the upper bits of
an Rn destination. The operation leaves FLAGS unchanged.
