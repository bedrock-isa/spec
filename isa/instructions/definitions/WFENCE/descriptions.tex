Prevents retirement until every earlier memory write is ordered before every later memory write on the same (:term:base.terms.logical_processor:). It performs no (:term:base.terms.memory_access:) and changes no register or flag.
