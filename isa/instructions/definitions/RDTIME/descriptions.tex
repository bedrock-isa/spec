RDTIME writes one atomic snapshot of the executing (:term:base.terms.logical_processor:)\textquotesingle{}s
64-bit invariant timebase to the destination (:term:base.terms.rn_register:). User and supervisor execution observe
the same timebase. RDTIME does not access memory, modify FLAGS, or order memory operations.
