name: Increment
summary: Increments the selected-width destination modulo its width without changing FLAGS.
route: integer_unary
privileged: false
repeat:
  type: repcc
  observed_value: dst
operands:
  dst:
    role: destination
    access: read_write
    value_type: integer
