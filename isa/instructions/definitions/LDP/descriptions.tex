Loads one naturally aligned 16-byte memory value into the selected canonical register pair. Bits 63:0 go to the
even register and bits 127:64 go to the following odd register. Both registers commit together; a preceding
alignment or memory-access fault changes neither register.
