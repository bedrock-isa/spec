Atomically replaces the selected-width memory value with the original source-register value and returns the old
memory value in that register. The encoded memory-order selector governs the complete read-modify-write operation.
Unlike the memory form of (:ref:base.instructions.XCHG:), this instruction is one atomic memory operation.
