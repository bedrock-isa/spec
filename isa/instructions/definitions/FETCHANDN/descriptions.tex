Atomically replaces memory with \(\mathit{oldMemory} \mathbin{\&} \lnot \mathit{source}\) and returns the old memory
value in the source register. The encoded memory-order selector governs the complete operation.
