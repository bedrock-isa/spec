Extracts 	exttt{imm6(w)+1} bits beginning at bit 	exttt{imm6(l)} of the selected-width source and writes the
zero-extended field to the destination. An encoding is invalid when the selected field extends past the operand
width.
