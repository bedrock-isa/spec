Loads the selected-width memory value into the destination register. The load has acquire ordering: later
(:term:base.terms.memory_operation|plural:) by the same (:term:base.terms.logical_processor:) are ordered after it.
A sub-Q destination is zero-extended to 64 bits. The instruction does not impose release or global
sequentially consistent ordering.
