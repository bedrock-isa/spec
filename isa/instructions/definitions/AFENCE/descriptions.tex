Acts as a full cumulative fence. Every earlier memory read and write is ordered before every later memory read and write on the same (:term:base.terms.logical_processor:), and (:term:base.terms.memory_effect|plural:) observed before the fence are propagated before effects ordered after it. Every AFENCE participates in the single global sequentially consistent order. It performs no (:term:base.terms.memory_access:) and changes no register or flag.
