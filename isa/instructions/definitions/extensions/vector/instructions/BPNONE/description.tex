Pn is read as a complete packed predicate image, with every stored bit included
in the branch decision.  An all-zero image selects the target operand; any set
bit selects the next instruction address.
