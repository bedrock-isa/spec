The selected B, W, L, or Q element size spaces one significant predicate
position at the start of each logical lane.  PTRUE writes a new Pd image with
those positions set and every intervening position clear.
