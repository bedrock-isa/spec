operation: VTRUNCL
title: Vector Truncation to Long
summary: Copies the low L field from each Q source container.
public_instruction:
  mnemonic: VTRUNCL
execution_route: vector
privilege: unprivileged
repeat:
  kind: not_eligible
operands:
- {id: govern, role: governing_predicate, access: read, value_domain: predicate, profiles: [Pn]}
- {id: src, role: source, access: read, value_domain: vector, profiles: [Vn]}
- {id: dst, role: destination, access: read_write, value_domain: vector, profiles: [Vn]}
cases:
- id: all_forms
  applies_to:
    forms: [extralong.vtruncl.v80]
    selectors:
    - {domain: Q_ONLY, values: [Q]}
  additional_requirements: []
  predicate: {kind: none}
  flags: []
  events: []
  sail_entry: execute_VTRUNCL
artifacts:
  semantics: {path: semantics.sail, kind: sail}
  description: {path: description.tex, kind: tex}
  diagrams:
  - id: quad-low-long
    path: diagrams/quad-low-long.yaml
    kind: vector-example
    case: all_forms
    caption: VTRUNCL.Q low-longword truncation
    alt_text: "An illustrative Q-container VTRUNCL view: each active container copies the low source longword into the low destination longword and preserves the old destination high longword; inactive containers preserve the complete old destination value."
