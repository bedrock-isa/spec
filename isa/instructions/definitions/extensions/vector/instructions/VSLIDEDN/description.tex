For each active destination lane, VSLIDEDN reads the old destination lane whose
index is the destination-lane index plus the unsigned count.  It writes zero
when that index reaches or exceeds the vector lane count.  The governing
predicate retains the corresponding old destination value in each inactive
lane.
