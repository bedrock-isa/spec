Each active unsigned L or Q source element is converted to a D element and
rounded according to the floating-point environment. The result is written
into the low double-precision field of its destination container. Every
inactive destination container retains its previous value.
