VEXTSW treats the low B source element in each W container as a signed
two's-complement value and sign-extends it across the destination W element.
The governing predicate position associated with each W container controls
that container.  An inactive container retains its complete previous
destination value.  The operation leaves FLAGS unchanged.
