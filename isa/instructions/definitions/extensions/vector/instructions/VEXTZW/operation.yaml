operation: VEXTZW
title: Vector Zero Extension to Word
summary: Zero-extends B source elements to W destination elements.
public_instruction:
  mnemonic: VEXTZW
execution_route: vector
privilege: unprivileged
repeat:
  kind: not_eligible
operands:
- {id: govern, role: governing_predicate, access: read, value_domain: predicate, profiles: [Pn]}
- {id: src, role: source, access: read, value_domain: vector, profiles: [Vn]}
- {id: dst, role: destination, access: read_write, value_domain: vector, profiles: [Vn]}
cases:
- id: all_forms
  applies_to:
    forms: [extralong.vextzw.v72]
    selectors:
    - {domain: B_ONLY, values: [B]}
  additional_requirements: []
  predicate: {kind: none}
  flags: []
  events: []
  sail_entry: execute_VEXTZW
artifacts:
  semantics: {path: semantics.sail, kind: sail}
  description: {path: description.tex, kind: tex}
  diagrams:
  - id: zero-byte-to-word
    path: diagrams/zero-byte-to-word.yaml
    kind: vector-example
    case: all_forms
    caption: VEXTZW.B unsigned byte-to-word extension
    alt_text: "An illustrative B-to-W VEXTZW view: each active word container zero-extends its low source byte, while inactive containers preserve the old destination value."
