VEXTZQ zero-extends the selected B, W, or L source element into its Q destination
container.  The governing predicate position associated with each Q container
controls that container.  An inactive container retains its complete previous
destination value.  The operation leaves FLAGS unchanged.
