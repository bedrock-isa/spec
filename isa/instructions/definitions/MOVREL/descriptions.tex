Stores the selected-width source register value to memory. The store has release ordering: earlier
(:term:base.terms.memory_operation|plural:) by the same (:term:base.terms.logical_processor:) are ordered before it.
The instruction does not impose acquire or global sequentially consistent ordering.
