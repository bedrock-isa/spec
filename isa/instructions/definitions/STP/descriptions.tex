Stores the selected canonical register pair as one naturally aligned 16-byte memory value. The even register
supplies bits 63:0 and the following odd register supplies bits 127:64. The two halves form one compound store;
a preceding alignment or memory-access fault exposes no partial write.
