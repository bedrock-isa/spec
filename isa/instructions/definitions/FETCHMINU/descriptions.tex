Atomically compares memory and source as unsigned selected-width integers, stores the lesser value, and returns the
old memory value in the source register. The encoded memory-order selector governs the complete operation.
