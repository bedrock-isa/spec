name: Read Flags
summary: Read the integer condition-code flags into an Rn register.
route: system_registers
privileged: false
operands:
  dst:
    role: destination
    access: write
    value_type: integer
