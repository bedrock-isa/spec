Invalidates every non-global cached translation for the selected 16-bit ASID on the current (:term:base.terms.logical_processor:), discards matching in-progress results, and prevents later use of stale entries.
