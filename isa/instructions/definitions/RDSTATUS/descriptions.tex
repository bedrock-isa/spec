RDSTATUS reads the architectural 16-bit STATUS register, zero-extends it to 64 bits, and writes the result to the destination (:term:base.terms.rn_register:). Reading STATUS is unprivileged; writing STATUS remains supervisor-only.
