Updates the low 32 bits of the destination as a reflected CRC-32 accumulator using polynomial
	exttt{0xEDB88320}. Source bytes are consumed from least-significant to most-significant; the instruction applies
no implicit initial or final XOR. The result is zero-extended to 64 bits.
