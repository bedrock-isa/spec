Prevents retirement until every earlier memory read is ordered before every later memory read on the same (:term:base.terms.logical_processor:). It performs no (:term:base.terms.memory_access:) and changes no register or flag.
