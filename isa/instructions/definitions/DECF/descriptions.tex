Subtracts one from the selected-width destination, writes the modular result, and updates (:ref:base.registers.STATE.FLAGS.Z:), (:ref:base.registers.STATE.FLAGS.N:), (:ref:base.registers.STATE.FLAGS.C:), and (:ref:base.registers.STATE.FLAGS.V:). The written destination result supplies the REPcc observed value.
