When the encoded condition is true, requires LPA.A to be zero, pushes the following instruction\textquotesingle{}s (:term:base.terms.continuation:) PC as one 64-bit stack value, and transfers control to the target. When the condition is false, execution continues without resident-link checking, target access, or stack access.
