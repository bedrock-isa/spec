Computes the selected-width bitwise conjunction only to set (:ref:base.registers.STATE.FLAGS.Z:) and (:ref:base.registers.STATE.FLAGS.N:), clears (:ref:base.registers.STATE.FLAGS.C:) and (:ref:base.registers.STATE.FLAGS.V:), and does not write the temporary result. The temporary conjunction supplies the REPcc observed value.
