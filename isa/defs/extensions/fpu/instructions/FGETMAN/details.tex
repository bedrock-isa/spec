For a normal finite source, the result preserves the sign and fraction and replaces the exponent with the bias, producing a magnitude in \([1,2)\). For zero, subnormal, infinity, and NaN encodings, the instruction leaves the destination behavior defined by the original special encoding rather than inserting an implicit leading one. The S and D forms use their native exponent and fraction widths.
