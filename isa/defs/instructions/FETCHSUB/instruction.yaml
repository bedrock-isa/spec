mnemonic: FETCHSUB
title: Atomic Fetch Subtract
summary: Performs the atomic fetch subtract operation.
description: Atomically replaces the memory operand with the selected-width result of old memory minus the source, and returns the old memory value in the source register.
attributes:
  class: system
  family: atomics
  privilege: unprivileged
