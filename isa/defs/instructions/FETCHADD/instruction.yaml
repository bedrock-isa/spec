mnemonic: FETCHADD
title: Atomic Fetch Add
summary: Performs the atomic fetch add operation.
description: Atomically replaces the memory operand with the selected-width sum of the old memory value and the source, and returns the old memory value in the source register.
attributes:
  class: system
  family: atomics
  privilege: unprivileged
