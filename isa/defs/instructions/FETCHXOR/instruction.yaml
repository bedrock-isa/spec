mnemonic: FETCHXOR
title: Atomic Fetch Xor
summary: Performs the atomic fetch xor operation.
description: Atomically replaces the memory operand with the selected-width bitwise exclusive-or of the old memory value and the source, and returns the old memory value in the source register.
attributes:
  class: system
  family: atomics
  privilege: unprivileged
