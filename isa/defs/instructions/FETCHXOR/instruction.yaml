mnemonic: FETCHXOR
title: Atomic Fetch Xor
summary: Performs the atomic fetch xor operation.
description: Atomically replaces the memory operand with its bitwise exclusive-or with the source and returns the previous memory value in the source register.
attributes:
  class: system
  family: atomics
  privilege: unprivileged
