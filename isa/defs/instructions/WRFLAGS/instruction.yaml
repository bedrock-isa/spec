mnemonic: WRFLAGS
title: Write Flags
summary: Write the integer condition-code flags from an Rn register.
description: WRFLAGS writes the low 16 bits from the source Rn register into the architectural FLAGS register. Reserved FLAGS bits must be zero.
attributes:
  class: system
  family: system_registers
  privilege: unprivileged
