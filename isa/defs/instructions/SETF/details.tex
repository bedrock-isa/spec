\begin{manualflageffects}{FLAGS}
\manualflageffect{Z}{set when mask bit 3 is one; otherwise unchanged}
\manualflageffect{N}{set when mask bit 2 is one; otherwise unchanged}
\manualflageffect{C}{set when mask bit 1 is one; otherwise unchanged}
\manualflageffect{V}{set when mask bit 0 is one; otherwise unchanged}
\end{manualflageffects}
