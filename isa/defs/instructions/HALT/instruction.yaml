mnemonic: HALT
title: Halt
summary: Halt the executing logical processor until an asynchronous architectural event is admitted.
description: Retires with the next instruction as its continuation and stops instruction fetch on the executing logical processor until an asynchronous architectural event is admitted and delivered.
attributes:
  class: system
  family: core_control
  privilege: supervisor
additional_description: details.tex
