mnemonic: MULHU
title: Unsigned Multiply High
summary: Performs the unsigned multiply high operation.
description: Multiplies the 64-bit operands as unsigned integers and writes the high 64 bits of the 128-bit product.
attributes:
  class: integer
  family: integer_mul_div
  privilege: unprivileged
repeat:
  contexts:
  - REP
  - REPcc
  - REPG
  observed:
    kind: result
    operand: dst
