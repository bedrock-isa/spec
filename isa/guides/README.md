# Bedrock programming and toolchain guides

The documents in this directory are non-normative. They collect worked
examples and implementation guidance without defining ISA, C ABI, ELF ABI, or
compiler-interface contracts.

`bedrock-programming-toolchain-guide.tex` owns the C-library and runtime
instruction examples that are intentionally excluded from the generated ISA
reference.
