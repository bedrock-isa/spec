\clearpage
\section{C Library Instruction Examples}

This section shows how the instruction set maps onto common C library kernels. The examples are schematic assembly
sequences rather than ABI-mandated entry points.

The examples use R0 as a source pointer, R1 as a destination pointer, R2 as the repeat counter, and R3 as a
temporary or fill-value register.

\subsection{Repeat Counter Convention}
Repeated scalar forms interpret the selected Rn register as an unsigned remaining count. A zero counter decodes
and checks the body, then annuls it with no architectural side effects. Every completed body iteration decrements
the counter by one.

The same architectural Rn value is visible to indexed effective-address calculation. Address update is part of the
EA syntax; after each successful element access, the selected base register advances by the element size.

\subsection{memcpy}
The forward byte-copy form uses source and destination base pointers and an unsigned byte count.

\input{isa/guides/fragments/memcpy_example.tex}

\subsection{memmove}
memmove chooses direction according to overlap. Non-overlapping and forward copies use the same shape as memcpy;
overlapping reverse copies use the matching predecrement address form.

\input{isa/guides/fragments/memmove_example.tex}

\subsection{memset}
memset uses a register source and a memory destination. The counter and auto-update convention is the same as the
forward copy traversal.

\input{isa/guides/fragments/memset_example.tex}

\subsection{memcmp}
memcmp can use a repeated comparison form to continue while the observed comparison result is equal. The final FLAGS
value is the result of the last completed comparison.

\input{isa/guides/fragments/memcmp_example.tex}

\subsection{Bulk Character Extension}
The orthogonal EXTS/EXTZ memory forms let a C library or codec routine widen elements while copying.

\input{isa/guides/fragments/bulk_extension_example.tex}
