name: Floating-Point Pop Register Pair
summary: Pops one canonical floating-point register pair selected by an inline pair index.
route: fpu
privileged: false
repeat:
  type: rep
operands:
  pair_id:
    role: source
    access: read
    value_type: integer
