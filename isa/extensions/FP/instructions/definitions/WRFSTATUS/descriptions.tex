WRFSTATUS writes only the architectural 16-bit FSTATUS register from the low 16 bits of the source (:term:base.terms.rn_register:). Reserved FSTATUS bits (:term:base.terms.must_be_zero:). WRFSTATUS requires the floating-point extension.
