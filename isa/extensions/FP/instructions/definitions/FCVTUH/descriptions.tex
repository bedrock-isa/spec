Converts between the H conversion format in an Fn operand and an unsigned integer of the suffix width in an Rn or
memory operand. Integer-to-H conversion uses (:ref:FP.registers.STATE.FSTATUS.RM:); H-to-integer conversion truncates toward zero.
