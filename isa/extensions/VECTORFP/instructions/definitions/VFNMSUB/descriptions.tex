Each active H, S, or D lane forms the negated fused multiply-subtract
expression from its two source elements and old accumulator destination
element. The complete expression has unbounded intermediate range and
precision and is rounded only once when written to the destination. An
inactive accumulator lane retains its previous value.
