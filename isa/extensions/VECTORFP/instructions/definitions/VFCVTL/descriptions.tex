Each active H, S, or D source element is converted toward zero to a signed
32-bit longword under the common floating-point conversion rules. The result
is written into the low longword of its destination container. Bits outside
that result (:term:base.terms.field:) and every inactive destination container retain their
previous values.
