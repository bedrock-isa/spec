name: Vector Floating-Point Duplicate
summary: Broadcasts a floating-point scalar register to every vector lane.
route: vector
privileged: false
operands:
  src:
    role: source
    access: read
    value_type: floating
  dst:
    role: destination
    access: write
    value_type: vector
