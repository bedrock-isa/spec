The governing predicate selects the active H, S, or D source elements.
The register form reads them from Vn, and the memory form reads them from the
corresponding active VEA locations. VFREDMIN reduces the selected elements
under the scalar floating-point minimum rules and writes the selected-format
result to Fn. If no element is selected, the result is positive infinity.
