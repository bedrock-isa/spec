Each active H, S, or D source element is converted toward zero to a signed
64-bit quadword under the common floating-point conversion rules. The result
is written into the low quadword of its destination container. Every inactive
destination container retains its previous value.
