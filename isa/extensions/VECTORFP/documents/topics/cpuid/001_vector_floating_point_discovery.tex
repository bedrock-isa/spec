\subsection{Vector Floating-Point Discovery}

Software establishes \texttt{VECTORFP}, \texttt{VECTOR}, and \texttt{FP} through the
\hyperref[section:cpuid-extension-directory]{Optional-Extension Directory} before using this extension. This
extension defines no additional discovery payload, so its discovery leaf contains only the common
(:term:base.terms.header:).

(:cpuid-leaf:VECTORFP.cpuid.EXTENSIONS.VECTORFP_DISCOVERY:)
