RDVSTATUS reads the architectural 16-bit VSTATUS register, zero-extends it to 64 bits, and writes the result to the destination (:term:base.terms.rn_register:). VSTATUS selects the current vector length. RDVSTATUS requires the vector extension.
