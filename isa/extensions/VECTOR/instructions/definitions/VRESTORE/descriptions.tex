VRESTORE validates the complete MAX\_VLEN-dependent source range and its bitmap before atomically replacing vector user state. A clear bit installs the corresponding initial value without reading that payload image. An all-clear bitmap makes vector state clean; any set bit makes it modified.
