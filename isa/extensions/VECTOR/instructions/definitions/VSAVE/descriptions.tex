VSAVE writes one 64-byte-aligned vector user-state record sized from MAX\_VLEN. A clean initial vector state may use an all-clear presence bitmap; otherwise VSAVE may conservatively mark and write every vector, predicate, and VSTATUS image. VSAVE does not change clean or modified state.
