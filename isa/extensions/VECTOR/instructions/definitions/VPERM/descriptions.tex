VPERM reads an element index from each active source lane.  An index within the
selected vector lane count copies the correspondingly numbered old destination
lane to that destination lane; an out-of-range index writes zero.  The
governing predicate retains the old destination value in each inactive lane.
