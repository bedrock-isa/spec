The governing predicate selects the active H, S, or D source elements.
The register form reads them from Vn, and the memory form reads them from the
corresponding active VEA locations. VREDMAX reduces the selected elements
under the scalar floating-point maximum rules and writes the selected-format
result to Fn. If no element is selected, the result is negative infinity.
