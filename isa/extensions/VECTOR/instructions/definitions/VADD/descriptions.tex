VADD applies selected integer addition independently to each active vector
lane. The governing predicate selects active lanes; an inactive destination
lane retains its previous value.

For B, W, L, and Q elements, each active result is the lane-width modular sum
and no flag bank changes.
