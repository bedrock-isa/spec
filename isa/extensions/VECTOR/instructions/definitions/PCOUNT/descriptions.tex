The selected element size maps Pn to one significant predicate position per
logical element.  PCOUNT scans those positions and writes the number of set
significant positions to Rn.  Bits between significant positions lie outside
the count.
