For each active destination lane whose index is at least the unsigned count,
VSLIDEUP reads the old destination lane at the index reduced by that count.  It
writes zero to an active lane with a lower index.  The governing predicate
retains the corresponding old destination value in each inactive lane.

(:diagram:VECTOR.instructions.VSLIDEUP.diagrams.slide-up-by-two:)
