mnemonic: PHEAD
name: Predicate Head
summary: Sets predicate positions from the count remainder through the final lane.
route: vector
sail_entries:
- execute_PHEAD
privileged: false
assembly:
  width_suffix_aliases: true
operands:
  count:
    role: count
    access: read
    value_type: integer
  dst:
    role: destination
    access: write
    value_type: predicate
