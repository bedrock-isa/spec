The governing predicate selects the contiguous memory elements to read. Each
active lane receives its corresponding memory element, and each inactive lane
receives zero. The resulting elements replace the destination vector image at
the selected width.
