Pn is read as a complete packed predicate image, so every stored bit
participates in the branch decision.  A set bit selects the target operand; an
all-zero image selects the next (:term:base.terms.instruction_address:).
