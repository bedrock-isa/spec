Pn is read as a complete current predicate execution image. Every predicate
bit below VLEN/8 participates in the branch decision; higher storage bits do
not. A set bit selects the target operand; an all-zero current image selects
the next (:term:base.terms.instruction_address:).
