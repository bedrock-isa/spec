For each adjacent result-lane pair, VTRNLO selects the even lane of the
corresponding pair in both inputs.  The even result lane receives that old
destination value, and the odd result lane receives the value from Vn.  The
governing predicate retains the corresponding old destination value in each
inactive result lane.

(:diagram:VECTOR.instructions.VTRNLO.diagrams.even-lane-transpose:)
