Pn supplies one significant predicate position for each lane of the selected
B, W, L, or Q element size.  The target operand is selected when all of those
positions are set; otherwise execution continues at the next instruction.  The
branch decision is derived solely from the significant positions.
