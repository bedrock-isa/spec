Pc, Ps, and the previous value of Pd are read as complete packed predicate
images.  At each bit position, a set control bit writes the corresponding Ps
bit to Pd, while a clear control bit preserves the previous Pd bit.  Selection
spans every bit of the packed image.
