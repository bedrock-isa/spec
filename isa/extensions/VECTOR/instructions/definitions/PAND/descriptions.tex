Ps and the previous value of Pd are read as complete packed predicate images.
Each result bit is the logical AND of the corresponding source and destination
bits.  The result replaces the complete Pd image, including both significant
and intervening bit positions.
