VLCNT writes the selected vector lane count to the complete 64-bit \texttt{Rn}
destination.  The count is VLEN in bytes divided by the selected B, W, L, or Q
element width in bytes.  The operation leaves FLAGS unchanged.
