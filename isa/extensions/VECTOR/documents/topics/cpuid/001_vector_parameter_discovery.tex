\subsection{Vector-Parameter Discovery}
After establishing \texttt{VECTOR} availability, software uses this leaf to obtain the architectural VLEN. VLEN is
$2^{\mathtt{VLEN\_LOG2\_BITS}}$ bits and is an implementation-selected power of two from 128 through 2048 bits.
It determines vector-register width, predicate storage, the number of lanes for a selected element width, and the
size of each (:term:VECTOR.terms.vector_register:) and (:term:VECTOR.terms.predicate_register:) image.
\texttt{SAVE\_AREA\_LAYOUT} remains the authority for save-component placement and size. This leaf does not
establish availability of \texttt{VECTORFP} or any other extension that consumes vector state.

(:cpuid-leaf:VECTOR.cpuid.EXTENSIONS.VECTOR_PARAMETERS:)
